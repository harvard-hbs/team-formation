# Environment variables for Team Formation container

# Production mode - enables static file serving and production CORS settings
PRODUCTION=true

# CORS origins - comma-separated list of allowed origins (optional)
# If not set in production mode, allows all origins (*)
# CORS_ORIGINS=https://example.com,https://app.example.com

# Port configuration (default: 8000)
# PORT=8000

# Logging level (default: warning)
# LOG_LEVEL=warning
